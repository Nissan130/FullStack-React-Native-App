import { Stack } from "expo-router";


// Root layout for the app, which ensures proper routing
export default function RootLayout() {
  return <Stack screenOptions={{headerShown:false}}>

  </Stack>;
}
